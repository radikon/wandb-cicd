import wandb
import ghapi

print(f'The version of WandB is: {wandb.__version__}')

# we're going to do the first assignment in this same file. The first assignment is to
# make a GitHub Action workflow that labels a PR with "bug" after you make the comment "/bug"
